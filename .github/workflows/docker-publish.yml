name: Docker Publish

# Build and push the MCP server image to Docker Hub, and keep the Hub repo
# overview in sync with README.md.
#
# - image       → only on version tags (v1.2.3): publishes :1.2.3, :1.2, :1, :latest
# - description → on pushes to main and manual runs: syncs README.md to the
#                 Docker Hub overview. This job never builds an image, so no
#                 rolling :main / :edge tags are created.
#
# Requires two repository secrets:
#   DOCKERHUB_USERNAME — Docker Hub account / org
#   DOCKERHUB_TOKEN    — a Docker Hub access token (Account Settings → Security)
#                        with read/write scope (read-only fails the description sync)

on:
  push:
    branches: [main]
    tags: ["v*.*.*"]
  workflow_dispatch:

env:
  IMAGE: ${{ secrets.DOCKERHUB_USERNAME }}/orionbelt-semantic-layer-mcp

# Docker Hub is authenticated with DOCKERHUB_TOKEN, not the GITHUB_TOKEN, so
# nothing here needs to write to the repository. No job raises this.
permissions:
  contents: read

jobs:
  image:
    runs-on: ubuntu-latest
    # Build/push only for released version tags — never for branch or PR pushes.
    if: startsWith(github.ref, 'refs/tags/v')
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Verify every Action is pinned to its named release
        run: ./scripts/check-action-pins.sh

      - name: Set up QEMU
        uses: docker/setup-qemu-action@96fe6ef7f33517b61c61be40b68a1882f3264fb8  # v4.2.0

      - name: Set up Buildx
        uses: docker/setup-buildx-action@37fe631027851001ddb9b187196cc803df7f5f0e  # v4.3.0

      - name: Log in to Docker Hub
        uses: docker/login-action@dbcb813823bdd20940b903addbd779551569679f  # v4.6.0
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Derive tags and labels
        id: meta
        uses: docker/metadata-action@dc802804100637a589fabce1cb79ff13a1411302  # v6.2.0
        with:
          images: ${{ env.IMAGE }}
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=semver,pattern={{major}}
          labels: |
            org.opencontainers.image.title=OrionBelt Semantic Layer MCP
            org.opencontainers.image.description=Thin MCP server that delegates to the OrionBelt Semantic Layer REST API.

      - name: Build and push
        uses: docker/build-push-action@53b7df96c91f9c12dcc8a07bcb9ccacbed38856a  # v7.3.0
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  description:
    runs-on: ubuntu-latest
    # Sync the README to the Hub overview on main pushes, manual runs, and
    # releases. Independent of the image build — no image is produced here.
    if: github.event_name != 'pull_request'
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1

      - name: Verify every Action is pinned to its named release
        run: ./scripts/check-action-pins.sh

      - name: Sync Docker Hub description
        uses: peter-evans/dockerhub-description@1b9a80c056b620d92cedb9d9b5a223409c68ddfa  # v5.0.0
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}
          repository: ${{ env.IMAGE }}
          short-description: Thin MCP server that delegates to the OrionBelt Semantic Layer REST API.
          readme-filepath: ./README.md
